{% extends 'base.html.twig' %}

{% block body %}
    <h1>Suroo</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ suroo.id }}</td>
            </tr>
            <tr>
                <th>Difficulty</th>
                <td>{{ suroo.difficulty }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('suroo_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('suroo_edit', { 'id': suroo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
