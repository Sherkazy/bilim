{% extends 'base.html.twig' %}

{% block body %}
    <h1>Bilim</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bilim.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('bilim_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('bilim_edit', { 'id': bilim.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
